export function DialogueScene({event,index,onNext,onChoose}:{event:any;index:number;onNext:()=>void;onChoose:(c:any)=>void}){const line=event.dialogue[index];const done=index>=event.dialogue.length-1;return <div className='scene-card'><p>{line.speaker}</p><p>{line.text}</p>{!done?<button onClick={onNext}>다음</button>:<div>{event.choices.map((c:any)=><button key={c.id} onClick={()=>onChoose(c)}>{c.text}</button>)}</div>}</div>}
